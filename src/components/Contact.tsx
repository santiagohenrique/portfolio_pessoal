import { Form } from "./Form";

export const Contact = () => {
    return(
        <section id="contato">
            <h2 className="contact-title">Contato</h2>
            <Form />
        </section>
    );
}